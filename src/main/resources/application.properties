# Puerto y conexión
quarkus.http.port=8080
quarkus.http.host=0.0.0.0

# Conexión a PostgreSQL (usa el nombre del servicio Docker: postgres)
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=postgres
quarkus.datasource.password=admin
quarkus.datasource.jdbc.url=jdbc:postgresql://postgres:5432/testdb

# Hibernate
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.log.sql=true

# REST path (actualizado para RESTEasy Reactive)
quarkus.http.root-path=/api

# Health (si tienes la dependencia, ya la tienes en el POM 👍)
quarkus.smallrye-health.root-path=/health
